apiVersion: v1
kind: Service
metadata:
  name: {{ app.name }}-frontend
  namespace: {{ namespace }}
  labels:
    app: {{ app.name }}
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: {{ app.name }}
    component: frontend
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
